# ============================================
# REQUIRED: OpenAI API Configuration
# ============================================
OPENAI_API_KEY=sk-your-api-key-here

# ============================================
# AI Model Configuration
# ============================================
# MODEL_ANALYZER: Cheaper/faster model for analysis and initial generation
MODEL_ANALYZER=gpt-4o-mini

# MODEL_GENERATOR: More powerful model for retries and complex cases
MODEL_GENERATOR=gpt-4o

# ============================================
# Retry Configuration
# ============================================
# Maximum number of retry attempts if validation fails
MAX_RETRIES=3

# ============================================
# Custom Instructions (Optional)
# ============================================
# Instructions for the analyzer stage (file selection)
DOCKAI_ANALYZER_INSTRUCTIONS="Always include package-lock.json if it exists."

# Instructions for the generator stage (Dockerfile creation)
DOCKAI_GENERATOR_INSTRUCTIONS="Ensure all images use multi-stage builds."

# ============================================
# Validation Configuration (Optional)
# ============================================
# Resource limits for container validation
# Adjust these for different application types (e.g., Java needs more memory)
DOCKAI_VALIDATION_MEMORY=512m
DOCKAI_VALIDATION_CPUS=1.0
DOCKAI_VALIDATION_PIDS=100

# Maximum allowed image size in MB (0 to disable check)
# Default: 500MB (realistic for production apps)
DOCKAI_MAX_IMAGE_SIZE_MB=500

# ============================================
# Security Configuration (Optional)
# ============================================
# Skip Trivy security scan entirely (not recommended)
DOCKAI_SKIP_SECURITY_SCAN=false

# Strict security mode: fail build on any CRITICAL/HIGH vulnerabilities
# Default: false (only fails on base image vulnerabilities, warns on app vulnerabilities)
DOCKAI_STRICT_SECURITY=false
